import {StyleSheet, View} from 'react-native';

const Verify = () => {
  return (
    <View>
      <Text>Verify</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Verify;
